# Supersocks URL Scraper public deployment template
# Copy this file to .env, then change values marked CHANGE_ME.
# Do not commit your real .env, browser-profiles/, data/, cookies, or tokens.

# Optional. If set, POST /summarize, /read, and /markdown require:
#   Authorization: Bearer <token>
API_BEARER_TOKEN=CHANGE_ME_RANDOM_LONG_TOKEN

# Default summary length when callers omit length.
DEFAULT_SUMMARY_LENGTH=900

# Production-grade fallback posture for hostile/JS/paywall-heavy media.
# The server still tries fast HTTP/SEO first; browser is fallback, not first route.
BROWSER_FALLBACK=1
BROWSER_PROFILE_DIR=/browser-profiles/default
BROWSER_POST_LOAD_WAIT_MS=10000
BROWSER_MAX_CONCURRENCY=1

# Public fallback layers.
SEO_FALLBACK=1
ARCHIVE_FALLBACK=1
FETCH_STRATEGY_CACHE_PATH=/data/fetch-strategies.json

# Opt-in Jina Reader fallback for LinkedIn/public pages after generic pipeline
# returns error/partial. Disabled by default. Never used for credentialed URLs,
# localhost/private hosts, or non-HTTP(S). No cookies/tokens are forwarded.
JINA_FALLBACK=0

# Optional documentary extraction (install extras: documents and/or pdf).
# Local OSS engines only: anydoc, pdf-inspector, PyMuPDF. No cloud OCR.
DOCUMENT_MAX_PAGES=50

# Optional X/Twitter via upstream twitter-cli (not a package extra).
# Set BOTH in the process environment before reading x.com / twitter.com URLs.
# Export from a manual Cookie-Editor dump only. This package never auto-reads
# browser cookies and never prints/stores these values.
# TWITTER_AUTH_TOKEN=
# TWITTER_CT0=

# Instagram/Facebook/Reddit use CloakBrowser first (browser extra).
# Optional persistent profile (cookies stay inside this directory only):
# SOCIAL_BROWSER_PROFILE_DIR=/browser-profiles/social
# BROWSER_PROFILE_DIR=/browser-profiles/default
# Headed warm-up under an existing DISPLAY/Xvfb (never auto-started here):
# CLOAK_HEADLESS=0
# DISPLAY=:99

# OpenCLI remains an opt-in desktop fallback only (never automatic):
# SOCIAL_OPENCLI_FALLBACK=0
# Install separately if needed: npm install -g @jackwener/opencli
# No OpenCLI tokens are stored here; keep the Browser Bridge extension enabled.

# Optional Reddit rdt-cli fallback (off by default; never auto-install/auto-cookie):
# RDT_CLI_FALLBACK=0

# Optional external summarizer. Leave local for standalone public operation.
# To use your own service: SUMMARY_PROVIDER=http and set SUMMARY_PROVIDER_URL.
SUMMARY_PROVIDER=local
SUMMARY_PROVIDER_URL=
SUMMARY_PROVIDER_TOKEN=
SUMMARY_PROVIDER_TIMEOUT=30

# Quiet noisy Chromium/Cloak font warnings in container logs.
CLOAKBROWSER_SUPPRESS_FONT_WARNING=1
